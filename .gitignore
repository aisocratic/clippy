node_modules/
npm-debug.log*
yarn-error.log*

# Built app bundles
dist/
out/
*.app

# The buddies are drawn, not stored: `npm run make-buddies` rebuilds them, and
# `npm start` does it for you if they are missing.
src/renderer/assets/

# macOS, editors, local tooling
.DS_Store
.idea/
.vscode/
.agent-grid/
.playwright-mcp/

# Downloaded sprite packs (third-party art, kept out of the repo)
sprites/

# Marketing site — source is tracked; generated assets (buddy GIFs copied from
# the app's themes, third-party sprite packs, cast-data.js) stay local: `npm run
# website` rebuilds them, and downloaded packs keep their own licences.
website/img/
website/cast-data.js

# Agent-session state (workflow worktrees, per-agent memory) — machine-local
.claude/worktrees/
.claude/agent-memory/
